{
  "name": "rxjs-store",
  "version": "6.0.0",
  "description": "an rxjs-based store solution intended for use in angular services",
  "main": "store.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["store", "rxjs", "angular"],
  "author": "Ahuvi Yearim",
  "license": "MIT",
  "dependencies": {
    "rxjs": "^6.0.0"
  }
}
